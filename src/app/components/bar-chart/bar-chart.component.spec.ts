/* tslint:disable:no-unused-variable */

import { BarChartComponent } from './bar-chart.component';

describe('Component: BarChart', () => {
  it('should create an instance', () => {
    let component = new BarChartComponent();
    expect(component).toBeTruthy();
  });
});
